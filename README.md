
  # Build Student and Teacher Prototype

  This is a code bundle for Build Student and Teacher Prototype. The original project is available at https://www.figma.com/design/rIelNP8MPLGLQ3NjOGrb7Y/Build-Student-and-Teacher-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  